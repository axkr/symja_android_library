# Seed corpus for the Weierstrass t=Tan(x/2) rational-trig stage.
#
# Format: `integrand ; var` (`; var` optional, defaults to x). `#` lines are comments.
# Author-chosen rational functions of Sin(x)/Cos(x).

1/(2 + Cos(x)) ; x
1/(1 + Sin(x)) ; x
1/(5 + 4*Cos(x)) ; x
1/(2 + Sin(x)) ; x
1/(3 + 5*Cos(x)) ; x
Sin(x)/(1 + Sin(x)) ; x
