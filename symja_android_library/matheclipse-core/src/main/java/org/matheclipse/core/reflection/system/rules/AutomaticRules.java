package org.matheclipse.core.reflection.system.rules;

import org.matheclipse.core.expression.BuiltInSymbol;
import org.matheclipse.core.expression.S;
import org.matheclipse.core.interfaces.IBuiltInSymbol;

/**
 * <p>
 * Generated by <code>org.matheclipse.core.preprocessor.RulePreprocessor</code>.
 * </p>
 * <p>
 * See GIT repository at:
 * <a href="https://github.com/axkr/symja_android_library">github.com/axkr/symja_android_library
 * under the tools directory</a>.
 * </p>
 */
public class AutomaticRules {
  private static final String PACKAGE = "org.matheclipse.core.reflection.system.rules.";

  /**
   * Defer the rules of <code>symbol</code> to the class initializer of the generated
   * <code>*Rules</code> class named <code>className</code>. A rule class can define rules or own
   * values for more than one symbol, so it may be registered at several symbols. Whichever symbol
   * is accessed first initializes the class and thereby installs the rules of all of them.
   */
  private static void defer(IBuiltInSymbol symbol, String className) {
    ((BuiltInSymbol) symbol).setDeferredRules(PACKAGE + className);
  }

  /**
   * Register the generated <code>*Rules</code> classes at the symbols they define. The rules of a
   * symbol are installed on the first access, so that a session only pays for the rules it really
   * uses.
   */
  public static void initialize() {
    defer(S.Abs, "AbsRules");
    defer(S.ArcCos, "ArcCosRules");
    defer(S.ArcCosh, "ArcCoshRules");
    defer(S.ArcCot, "ArcCotRules");
    defer(S.ArcCoth, "ArcCothRules");
    defer(S.ArcCsc, "ArcCscRules");
    defer(S.ArcCsch, "ArcCschRules");
    defer(S.ArcSec, "ArcSecRules");
    defer(S.ArcSech, "ArcSechRules");
    defer(S.ArcSin, "ArcSinRules");
    defer(S.ArcSinh, "ArcSinhRules");
    defer(S.ArcTan, "ArcTanRules");
    defer(S.ArcTanh, "ArcTanhRules");
    defer(S.BesselI, "BesselIRules");
    defer(S.BesselJ, "BesselJRules");
    defer(S.BesselK, "BesselKRules");
    defer(S.BesselY, "BesselYRules");
    defer(S.Conjugate, "ConjugateRules");
    defer(S.Cos, "CosRules");
    defer(S.Cosh, "CoshRules");
    defer(S.Cot, "CotRules");
    defer(S.Coth, "CothRules");
    defer(S.Csc, "CscRules");
    defer(S.Csch, "CschRules");
    defer(S.D, "DRules");
    defer(S.Derivative, "DerivativeRules");
    defer(S.Gamma, "GammaRules");
    defer(S.Gudermannian, "GudermannianRules");
    defer(S.HurwitzLerchPhi, "HurwitzLerchPhiRules");
    defer(S.Hypergeometric1F1, "Hypergeometric1F1Rules");
    defer(S.Hypergeometric2F1Regularized, "Hypergeometric2F1RegularizedRules");
    defer(S.Hypergeometric2F1, "Hypergeometric2F1Rules");
    defer(S.HypergeometricPFQ, "HypergeometricPFQRules");
    defer(S.HypergeometricU, "HypergeometricURules");
    defer(S.InterquartileRange, "InterquartileRangeRules");
    defer(S.InverseZTransform, "InverseZTransformRules");
    defer(S.JacobiSC, "JacobiSCRules");
    defer(S.KleinInvariantJ, "KleinInvariantJRules");
    defer(S.LaplaceTransform, "LaplaceTransformRules");
    defer(S.LerchPhi, "LerchPhiRules");
    defer(S.Limit, "LimitRules");
    defer(S.Log, "LogRules");
    defer(S.PolyGamma, "PolyGammaRules");
    defer(S.PolyLog, "PolyLogRules");
    defer(S.Power, "PowerRules");
    defer(S.ProductLog, "ProductLogRules");
    defer(S.Quantile, "QuantileRules");
    defer(S.Black, "RGBColorRules");
    defer(S.Blue, "RGBColorRules");
    defer(S.Brown, "RGBColorRules");
    defer(S.Cyan, "RGBColorRules");
    defer(S.Gray, "RGBColorRules");
    defer(S.Green, "RGBColorRules");
    defer(S.LightBlue, "RGBColorRules");
    defer(S.LightBrown, "RGBColorRules");
    defer(S.LightCyan, "RGBColorRules");
    defer(S.LightGray, "RGBColorRules");
    defer(S.LightGreen, "RGBColorRules");
    defer(S.LightMagenta, "RGBColorRules");
    defer(S.LightOrange, "RGBColorRules");
    defer(S.LightPink, "RGBColorRules");
    defer(S.LightPurple, "RGBColorRules");
    defer(S.LightRed, "RGBColorRules");
    defer(S.LightYellow, "RGBColorRules");
    defer(S.Magenta, "RGBColorRules");
    defer(S.Orange, "RGBColorRules");
    defer(S.Pink, "RGBColorRules");
    defer(S.Purple, "RGBColorRules");
    defer(S.RGBColor, "RGBColorRules");
    defer(S.Red, "RGBColorRules");
    defer(S.White, "RGBColorRules");
    defer(S.Yellow, "RGBColorRules");
    defer(S.Sec, "SecRules");
    defer(S.Sech, "SechRules");
    defer(S.Sin, "SinRules");
    defer(S.Sinc, "SincRules");
    defer(S.Sinh, "SinhRules");
    defer(S.SphericalBesselJ, "SphericalBesselJRules");
    defer(S.SphericalBesselY, "SphericalBesselYRules");
    defer(S.SphericalHarmonicY, "SphericalHarmonicYRules");
    defer(S.StandardDeviation, "StandardDeviationRules");
    defer(S.StieltjesGamma, "StieltjesGammaRules");
    defer(S.StruveH, "StruveHRules");
    defer(S.StruveL, "StruveLRules");
    defer(S.Tan, "TanRules");
    defer(S.Tanh, "TanhRules");
    defer(S.WhittakerM, "WhittakerMRules");
    defer(S.WhittakerW, "WhittakerWRules");
    defer(S.ZTransform, "ZTransformRules");
  }
}
